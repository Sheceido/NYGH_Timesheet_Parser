{
  "name": "usScheduleTimesheetAndChecker",
  "version": "0.9.0",
  "description": "Program to parse tab-separated US schedule and output the time sheet for a given technologist, and to check for errors in the schedule",
  "main": "",
"scripts": {
  "tsc:build": "tsc",
  "tsc:watch": "tsc --watch",
  "tsc:init": "tsc --init"
  "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "Sheceido",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^6.0.3"
  }
}
